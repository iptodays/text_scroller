name: text_scroller
description: A highly customizable text scrolling widget. You can customize all the aspects.
version: 1.0.0
homepage: https://github.com/iptodays/text_scroller

environment:
  sdk: ^3.6.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
